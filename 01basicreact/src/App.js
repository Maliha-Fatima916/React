//This App() is a React Component (a special function or class) that returns UI (written in JSX).
//This is the main component where all other components usually connect
//When you run your React app, index.js renders this <App /> component inside the root div of index.html.

function App() {
  return (
    <h1> First React App </h1> // UI => that will appear on browser
  );
}

export default App; // exporting it to import it in index.js
